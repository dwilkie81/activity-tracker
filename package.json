{
  "name": "activity-tracker",
  "version": "0.0.1",
  "description": "Simple activity tracker",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "typescript": "^3.4.3",
    "electron": "^10.1.5"
  },
  "dependencies": {
    "jquery": "^3.5.1",
    "sqlite3": "^5.0.0"
  }
}
